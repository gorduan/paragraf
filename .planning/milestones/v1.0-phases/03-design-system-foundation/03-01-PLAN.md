---
phase: 03-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/styles/index.css
  - frontend/src/lib/utils.ts
  - frontend/src/components/ui/Button.tsx
  - frontend/src/components/ui/Card.tsx
  - frontend/src/components/ui/Input.tsx
  - frontend/src/components/ui/Badge.tsx
  - frontend/vitest.config.ts
  - frontend/src/lib/utils.test.ts
  - frontend/src/components/ui/__tests__/Button.test.tsx
  - frontend/src/components/ui/__tests__/Badge.test.tsx
  - frontend/src/components/ui/__tests__/Card.test.tsx
  - frontend/src/components/ui/__tests__/Input.test.tsx
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "Design tokens for colors, spacing, typography, shadows, and radii are defined in TailwindCSS 4 @theme block"
    - "Primary palette is indigo (500: #6366f1), not blue"
    - "Button, Card, Input, Badge primitives exist with variant/size props"
    - "cn() utility merges Tailwind classes correctly"
    - "Frontend builds without TypeScript errors"
    - "Existing accessibility CSS (skip-link, sr-only, focus-visible, reduced-motion, high-contrast) is preserved"
  artifacts:
    - path: "frontend/src/styles/index.css"
      provides: "Complete design token system in @theme block"
      contains: "--color-primary-500: #6366f1"
    - path: "frontend/src/lib/utils.ts"
      provides: "cn() className merging utility"
      exports: ["cn"]
    - path: "frontend/src/components/ui/Button.tsx"
      provides: "Button primitive with variant/size props"
      exports: ["Button"]
    - path: "frontend/src/components/ui/Card.tsx"
      provides: "Card primitive with variant props"
      exports: ["Card"]
    - path: "frontend/src/components/ui/Input.tsx"
      provides: "Input primitive with variant props"
      exports: ["Input"]
    - path: "frontend/src/components/ui/Badge.tsx"
      provides: "Badge primitive with variant props"
      exports: ["Badge"]
  key_links:
    - from: "frontend/src/components/ui/Button.tsx"
      to: "frontend/src/lib/utils.ts"
      via: "import cn"
      pattern: "import.*cn.*from.*@/lib/utils"
    - from: "frontend/src/components/ui/Button.tsx"
      to: "frontend/src/styles/index.css"
      via: "token-based classes"
      pattern: "bg-primary-600|text-body|rounded-md"
---

<objective>
Install design system dependencies, define complete token palette in TailwindCSS 4 @theme block, create cn() utility, build 4 non-Radix primitive components (Button, Card, Input, Badge), and set up vitest test infrastructure.

Purpose: Establish the token foundation and simple primitives that all subsequent components and migrations depend on. Per D-01 through D-05.
Output: Token system in index.css, 4 primitive components in src/components/ui/, cn() utility, vitest config and initial tests.
</objective>

<execution_context>
@E:/Claude Projekte/Tobias/paragraf v2/.claude/get-shit-done/workflows/execute-plan.md
@E:/Claude Projekte/Tobias/paragraf v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-system-foundation/03-CONTEXT.md
@.planning/phases/03-design-system-foundation/03-RESEARCH.md
@.planning/phases/03-design-system-foundation/03-UI-SPEC.md

<interfaces>
<!-- Current index.css @theme block that must be REPLACED (blue -> indigo) and EXPANDED -->
<!-- See frontend/src/styles/index.css lines 5-16 for current @theme -->
<!-- All accessibility CSS after line 18 MUST be preserved exactly -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, set up vitest, create cn() utility, define design tokens</name>
  <files>
    frontend/package.json,
    frontend/src/styles/index.css,
    frontend/src/lib/utils.ts,
    frontend/vitest.config.ts,
    frontend/src/lib/utils.test.ts
  </files>
  <read_first>
    frontend/package.json,
    frontend/src/styles/index.css,
    frontend/vite.config.ts,
    frontend/tsconfig.json,
    .planning/phases/03-design-system-foundation/03-UI-SPEC.md,
    .planning/phases/03-design-system-foundation/03-RESEARCH.md
  </read_first>
  <action>
**Step 1: Install all dependencies in one command:**
```bash
cd frontend && npm install @radix-ui/react-dialog @radix-ui/react-select @radix-ui/react-tooltip @radix-ui/react-tabs class-variance-authority clsx tailwind-merge && npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom @testing-library/user-event
```

**Step 2: Create `frontend/vitest.config.ts`:**
```typescript
import { defineConfig } from "vitest/config";
import path from "path";

export default defineConfig({
  test: {
    environment: "jsdom",
    globals: true,
    setupFiles: [],
  },
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "src"),
    },
  },
});
```

**Step 3: Create `frontend/src/lib/utils.ts`** with cn() utility:
```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

**Step 4: Create `frontend/src/lib/utils.test.ts`:**
Test cn() merging: `cn("p-4", "p-6")` returns string containing `p-6` but not `p-4`. Test conditional: `cn("base", false && "hidden", "extra")` returns `"base extra"`. Test empty: `cn()` returns `""`.

**Step 5: Replace the `@theme` block in `frontend/src/styles/index.css`** with the full token system per D-01 and D-02. The file must start with `@import "tailwindcss";` then `@custom-variant dark` (existing), then the expanded `@theme` block. ALL CSS after the @theme block (skip-link, sr-only, focus-visible, reduced-motion, high-contrast, scrollbar) MUST be preserved exactly as-is.

The @theme block must contain EXACTLY these tokens (from UI-SPEC + RESEARCH):

**Primary Indigo (D-02):**
- `--color-primary-50: #eef2ff` through `--color-primary-900: #312e81` (10 steps: 50,100,200,300,400,500,600,700,800,900)
- `--color-primary-500: #6366f1` (the key indigo value)

**Neutral Slate:**
- `--color-neutral-50: #f8fafc` through `--color-neutral-950: #020617` (11 steps: 50,100,200,300,400,500,600,700,800,900,950)

**Semantic Colors (D-01) -- 5 steps each (50,100,500,600,900):**
- Success: 50:#f0fdf4, 100:#dcfce7, 500:#22c55e, 600:#16a34a, 900:#14532d
- Warning: 50:#fffbeb, 100:#fef3c7, 500:#f59e0b, 600:#d97706, 900:#78350f
- Error: 50:#fef2f2, 100:#fee2e2, 500:#ef4444, 600:#dc2626, 700:#b91c1c, 900:#7f1d1d
- Info: 50:#eef2ff, 100:#e0e7ff, 500:#6366f1, 600:#4f46e5, 900:#312e81

**Spacing (4px base, D-01):**
- `--spacing-xs: 4px`, `--spacing-sm: 8px`, `--spacing-xs2: 12px`, `--spacing-md: 16px`, `--spacing-lg: 24px`, `--spacing-xl: 32px`, `--spacing-2xl: 48px`, `--spacing-3xl: 64px`

**Typography (D-01) -- use `--text-*` namespace for TailwindCSS 4:**
- `--text-caption: 12px` + `--text-caption--line-height: 1.5`
- `--text-body: 14px` + `--text-body--line-height: 1.5`
- `--text-subheading: 16px` + `--text-subheading--line-height: 1.4`
- `--text-heading: 20px` + `--text-heading--line-height: 1.3`
- `--text-display: 28px` + `--text-display--line-height: 1.2`
- `--font-weight-regular: 400`
- `--font-weight-semibold: 600`

**Shadows (D-01):**
- `--shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05)`
- `--shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1)`
- `--shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)`

**Border Radii (D-01):**
- `--radius-sm: 4px`, `--radius-md: 8px`, `--radius-lg: 12px`

**CRITICAL:** Update the high-contrast media query (per Pitfall 5 in RESEARCH): replace `--color-primary-500: #1d4ed8` with `--color-primary-500: #3730a3` (darker indigo) and `--color-primary-600: #1e3a8a` with `--color-primary-600: #312e81` (darkest indigo).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run src/lib/utils.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/styles/index.css contains `--color-primary-500: #6366f1`
    - frontend/src/styles/index.css contains `--spacing-xs: 4px`
    - frontend/src/styles/index.css contains `--text-body: 14px`
    - frontend/src/styles/index.css contains `--shadow-md:`
    - frontend/src/styles/index.css contains `--radius-md: 8px`
    - frontend/src/styles/index.css contains `--color-success-500: #22c55e`
    - frontend/src/styles/index.css contains `--color-neutral-950: #020617`
    - frontend/src/styles/index.css contains `--font-weight-regular: 400`
    - frontend/src/styles/index.css contains `--font-weight-semibold: 600`
    - frontend/src/styles/index.css contains `@custom-variant dark`
    - frontend/src/styles/index.css contains `.skip-link` (accessibility preserved)
    - frontend/src/styles/index.css contains `.sr-only` (accessibility preserved)
    - frontend/src/styles/index.css contains `prefers-reduced-motion` (accessibility preserved)
    - frontend/src/styles/index.css high-contrast block contains `#3730a3` (indigo, not old blue #1d4ed8)
    - frontend/src/lib/utils.ts contains `export function cn`
    - frontend/src/lib/utils.ts contains `twMerge(clsx(inputs))`
    - frontend/vitest.config.ts contains `environment: "jsdom"`
    - frontend/package.json contains `class-variance-authority`
    - frontend/package.json contains `@radix-ui/react-dialog`
    - frontend/package.json contains `clsx`
    - frontend/package.json contains `tailwind-merge`
    - frontend/package.json contains `vitest`
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/lib/utils.test.ts` exits 0
  </acceptance_criteria>
  <done>All design tokens defined in @theme, cn() utility created and tested, vitest configured, all dependencies installed. High-contrast media query updated for indigo. All existing accessibility CSS preserved.</done>
</task>

<task type="tdd">
  <name>Task 2: Build Button, Card, Input, Badge primitives with cva variants</name>
  <files>
    frontend/src/components/ui/Button.tsx,
    frontend/src/components/ui/Card.tsx,
    frontend/src/components/ui/Input.tsx,
    frontend/src/components/ui/Badge.tsx,
    frontend/src/components/ui/__tests__/Button.test.tsx,
    frontend/src/components/ui/__tests__/Badge.test.tsx,
    frontend/src/components/ui/__tests__/Card.test.tsx,
    frontend/src/components/ui/__tests__/Input.test.tsx
  </files>
  <read_first>
    frontend/src/lib/utils.ts,
    frontend/src/styles/index.css,
    .planning/phases/03-design-system-foundation/03-UI-SPEC.md,
    .planning/phases/03-design-system-foundation/03-RESEARCH.md
  </read_first>
  <behavior>
    - Button: renders with `button` tag, applies primary variant classes by default, supports variant="secondary"|"ghost"|"destructive", supports size="sm"|"md"|"lg", forwards className via cn(), disabled renders with opacity-50
    - Badge: renders with `span` tag, applies default variant classes, supports variant="primary"|"success"|"warning"|"error", forwards className via cn()
    - Card: renders children, applies default variant classes (white bg, slate border, shadow-md, rounded-md), supports variant="interactive" with hover styles
    - Input: renders `input` tag, applies default styling, supports variant="error" with red border, forwards ref via React.forwardRef
  </behavior>
  <action>
**Button.tsx** (per D-05, UI-SPEC Button section):
- Import `cva, type VariantProps` from `class-variance-authority` and `cn` from `@/lib/utils`
- Define `buttonVariants` with cva:
  - Base: `"inline-flex items-center justify-center font-semibold transition-colors focus-visible:outline-none focus-visible:ring-3 focus-visible:ring-primary-500 focus-visible:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed"`
  - variant:
    - primary: `"bg-primary-600 text-white hover:bg-primary-500 active:bg-primary-700"`
    - secondary: `"bg-slate-100 text-slate-700 border border-slate-200 hover:bg-slate-200 dark:bg-slate-700 dark:text-slate-200 dark:border-slate-600 dark:hover:bg-slate-600"`
    - ghost: `"text-slate-600 hover:bg-slate-100 dark:text-slate-300 dark:hover:bg-slate-800"`
    - destructive: `"bg-red-500 text-white hover:bg-red-600 active:bg-red-700"`
  - size:
    - sm: `"h-8 px-xs2 text-caption rounded-md"`
    - md: `"h-10 px-md text-body rounded-md"`
    - lg: `"h-12 px-lg text-subheading rounded-md"`
  - defaultVariants: variant: "primary", size: "md"
- Interface `ButtonProps` extends `React.ButtonHTMLAttributes<HTMLButtonElement>` and `VariantProps<typeof buttonVariants>`
- Named export `export function Button({ className, variant, size, ...props }: ButtonProps)`
- Return `<button className={cn(buttonVariants({ variant, size }), className)} {...props} />`

**Card.tsx** (per UI-SPEC Card section):
- Define `cardVariants` with cva:
  - Base: `"rounded-md border p-lg"`
  - variant:
    - default: `"bg-white dark:bg-slate-800 border-slate-200 dark:border-slate-700 shadow-md"`
    - interactive: `"bg-white dark:bg-slate-800 border-slate-200 dark:border-slate-700 shadow-md hover:shadow-lg hover:bg-slate-50 dark:hover:bg-slate-750 transition-shadow cursor-pointer"`
  - defaultVariants: variant: "default"
- Interface extends `React.HTMLAttributes<HTMLDivElement>` and VariantProps
- Named export: `export function Card({ className, variant, ...props }: CardProps)`
- Also export `CardHeader`, `CardContent`, `CardFooter` as simple styled divs for compositional use

**Input.tsx** (per UI-SPEC Input section):
- Define `inputVariants` with cva:
  - Base: `"h-10 w-full px-xs2 bg-white dark:bg-slate-800 border rounded-md text-body placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-transparent disabled:opacity-50 disabled:cursor-not-allowed"`
  - variant:
    - default: `"border-slate-200 dark:border-slate-600"`
    - error: `"border-error-500 focus:ring-error-500"`
  - defaultVariants: variant: "default"
- Use `React.forwardRef<HTMLInputElement, InputProps>` so refs work
- Named export `export const Input = React.forwardRef<HTMLInputElement, InputProps>(...)`
- Set `Input.displayName = "Input"`

**Badge.tsx** (per UI-SPEC Badge section, RESEARCH example):
- Define `badgeVariants` with cva:
  - Base: `"inline-flex items-center px-xs py-0.5 text-caption font-semibold rounded-sm"`
  - variant:
    - default: `"bg-slate-100 text-slate-700 dark:bg-slate-700 dark:text-slate-200"`
    - primary: `"bg-primary-50 text-primary-700 dark:bg-primary-900 dark:text-primary-300"`
    - success: `"bg-success-50 text-success-700 dark:bg-success-900 dark:text-success-300"`
    - warning: `"bg-warning-50 text-warning-700 dark:bg-warning-900 dark:text-warning-300"`
    - error: `"bg-error-50 text-error-700 dark:bg-error-900 dark:text-error-300"`
  - defaultVariants: variant: "default"
- Named export `export function Badge({ className, variant, ...props }: BadgeProps)`

**Button.test.tsx:**
- Test: renders a button element
- Test: applies primary variant classes by default (check for `bg-primary-600`)
- Test: applies secondary variant classes when variant="secondary" (check for `border`)
- Test: applies size sm classes when size="sm" (check for `h-8`)
- Test: merges custom className
- Test: forwards onClick handler

**Badge.test.tsx:**
- Test: renders a span element
- Test: applies default variant classes (check for `bg-slate-100`)
- Test: applies primary variant (check for `bg-primary-50`)
- Test: renders children text

**Card.test.tsx:**
- Test: renders children content
- Test: applies default variant classes (check for `shadow-md`, `rounded-md`)
- Test: applies interactive variant with hover classes (check for `hover:shadow-lg`, `cursor-pointer`)
- Test: merges custom className

**Input.test.tsx:**
- Test: renders an input element
- Test: applies default variant border (check for `border-slate-200`)
- Test: applies error variant border (check for `border-error-500`)
- Test: forwards ref correctly (verify ref.current is an HTMLInputElement)
- Test: sets displayName to "Input"
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ui/Button.tsx contains `export function Button`
    - frontend/src/components/ui/Button.tsx contains `buttonVariants`
    - frontend/src/components/ui/Button.tsx contains `bg-primary-600`
    - frontend/src/components/ui/Button.tsx contains `variant: "primary"`
    - frontend/src/components/ui/Card.tsx contains `export function Card`
    - frontend/src/components/ui/Card.tsx contains `cardVariants`
    - frontend/src/components/ui/Card.tsx contains `shadow-md`
    - frontend/src/components/ui/Input.tsx contains `React.forwardRef`
    - frontend/src/components/ui/Input.tsx contains `inputVariants`
    - frontend/src/components/ui/Input.tsx contains `border-error-500`
    - frontend/src/components/ui/Input.tsx contains `displayName`
    - frontend/src/components/ui/Badge.tsx contains `export function Badge`
    - frontend/src/components/ui/Badge.tsx contains `badgeVariants`
    - frontend/src/components/ui/Badge.tsx contains `bg-success-50`
    - frontend/src/components/ui/__tests__/Button.test.tsx exists
    - frontend/src/components/ui/__tests__/Badge.test.tsx exists
    - frontend/src/components/ui/__tests__/Card.test.tsx exists
    - frontend/src/components/ui/__tests__/Input.test.tsx exists
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0 with all tests passing
  </acceptance_criteria>
  <done>4 primitive components (Button, Card, Input, Badge) created with cva variant support, typed props, cn() integration. Unit tests pass for all 4 components (Button, Badge, Card, Input).</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0 (TypeScript strict mode passes)
- `cd frontend && npm run build` exits 0 (full build succeeds)
- `cd frontend && npx vitest run --reporter=verbose` exits 0 (all unit tests pass)
- `frontend/src/styles/index.css` contains `--color-primary-500: #6366f1` (indigo, not blue)
- All accessibility CSS blocks preserved (skip-link, sr-only, focus-visible, reduced-motion, high-contrast)
</verification>

<success_criteria>
- Design tokens fully defined in @theme block covering: primary indigo (10 steps), neutral slate (11 steps), 4 semantic colors (5 steps each), spacing (8 values), typography (5 sizes + 2 weights), shadows (3 levels), radii (3 levels)
- cn() utility works correctly with tailwind-merge deduplication
- Button has 4 variants (primary/secondary/ghost/destructive) and 3 sizes (sm/md/lg)
- Card has 2 variants (default/interactive)
- Input has 2 variants (default/error) with ref forwarding
- Badge has 5 variants (default/primary/success/warning/error)
- vitest configured and running with jsdom environment
- Frontend builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-system-foundation/03-01-SUMMARY.md`
</output>
