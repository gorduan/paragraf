---
phase: 10
slug: dashboard-export-polish
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-28
---

# Phase 10 — UI Design Contract

> Visual and interaction contract for the final milestone phase: snapshot management, document export, accessibility compliance, and responsive polish. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (manual cva+cn system) |
| Preset | not applicable |
| Component library | Radix UI (Dialog, Select, Tooltip, Tabs) |
| Icon library | lucide-react ^0.460.0 |
| Font | System font stack (Tailwind 4 default) |

Source: Phase 3 design system foundation, `frontend/src/styles/index.css`, `frontend/package.json`

---

## Spacing Scale

Declared values (multiples of 4, using Tailwind 4 numeric scale):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px (1) | Icon gaps, inline padding |
| sm | 8px (2) | Compact element spacing, snapshot list item padding |
| md | 16px (4) | Default element spacing, card padding |
| lg | 24px (6) | Section padding, snapshot section margin-top |
| xl | 32px (8) | Layout gaps between major sections |
| 2xl | 48px (12) | Major section breaks |
| 3xl | 64px (16) | Page-level spacing |

Exceptions:
- 44px minimum touch targets on all interactive elements per WCAG 2.5.5 / D-14. Use `min-h-[44px] min-w-[44px]` on buttons, links, checkboxes that do not already meet this via size variants.
- Sidebar hamburger icon button: 44px square touch target.

Source: `frontend/src/styles/index.css` lines 60-64, D-14 from CONTEXT.md

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Subheading | 16px | 600 (semibold) | 1.4 |
| Heading | 20px | 600 (semibold) | 1.3 |
| Display | 28px | 600 (semibold) | 1.2 |

Usage in this phase:
- Snapshot section heading: `text-heading font-semibold` (20px/600)
- Snapshot list item name: `text-body font-semibold` (14px/600)
- Snapshot timestamp + file size: `text-body text-neutral-400` (14px/400, reduced opacity via muted color to create visual hierarchy without a separate size token)
- Export dropdown labels: `text-body` (14px/400)
- Accessibility audit: verify all text uses these tokens, no raw px values

Source: `frontend/src/styles/index.css` lines 67-78

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | neutral-50 `#f8fafc` (light) / neutral-950 `#020617` (dark) | Page background, main content area |
| Secondary (30%) | white / neutral-900 `#0f172a` (dark) | Cards, sidebar, snapshot list cards, export toolbar |
| Accent (10%) | primary-600 `#4f46e5` | Primary CTA buttons (Snapshot erstellen, Export), active nav item, focus rings |
| Destructive | error-500 `#ef4444` | Snapshot delete button, restore warning icon |
| Success | success-500 `#22c55e` | Snapshot creation success toast, restore complete indicator |
| Warning | warning-500 `#f59e0b` | Auto-snapshot toggle warning, restore confirmation dialog icon |

Accent reserved for:
- "Snapshot erstellen" primary button
- "Alles exportieren" primary button / dropdown trigger
- Active sidebar navigation item highlight
- Focus-visible ring on interactive elements
- Export format selection (active state)

Source: `frontend/src/styles/index.css` lines 6-58, Phase 3 design tokens

---

## Visual Hierarchy

### IndexPage Layout Anchor

The donut chart remains the primary visual focal point of the IndexPage. The SnapshotSection is secondary content placed below the existing dashboard stats area. On desktop (>= 1024px), the SnapshotSection sits below the fold — users scroll past the donut chart and stats cards to reach it. On mobile/tablet, the stacked layout naturally places snapshots further down.

### Snapshot Action Icons

Snapshot action icons (restore via `RotateCcw`, delete via `Trash2` from lucide-react) render as icon-only ghost buttons with a Radix Tooltip on hover/focus. Tooltip labels: "Wiederherstellen" and "Loeschen". Each icon button has a 44px minimum touch target. No text labels accompany the icons — the tooltip and aria-label provide the label.

---

## Copywriting Contract

### Snapshot Management

| Element | Copy |
|---------|------|
| Section heading | Snapshots |
| Create button | Snapshot erstellen |
| Create success | Snapshot "{name}" wurde erstellt |
| Create error | Snapshot konnte nicht erstellt werden. Bitte versuchen Sie es erneut. |
| Empty state heading | Keine Snapshots vorhanden |
| Empty state body | Erstellen Sie einen Snapshot, um den aktuellen Stand der Datenbank zu sichern. |
| Restore confirmation title | Snapshot wiederherstellen? |
| Restore confirmation body | Die aktuelle Datenbank wird durch den Stand von "{name}" ({timestamp}) ersetzt. Dieser Vorgang kann nicht rueckgaengig gemacht werden. |
| Restore button | Jetzt wiederherstellen |
| Restore success | Datenbank wurde aus Snapshot "{name}" wiederhergestellt |
| Delete confirmation title | Snapshot loeschen? |
| Delete confirmation body | Der Snapshot "{name}" wird unwiderruflich geloescht. |
| Delete button | Endgueltig loeschen |
| Auto-snapshot toggle label | Auto-Snapshot vor Indexierung |
| Auto-snapshot description | Erstellt automatisch einen Snapshot bevor eine Indexierung gestartet wird. Maximal {max} Snapshots werden aufbewahrt. |
| Timestamp format | Relative: "vor 2 Stunden", "vor 3 Tagen" (use Intl.RelativeTimeFormat) |

### Export

| Element | Copy |
|---------|------|
| Global export button | Alles exportieren |
| Format option PDF | Als PDF exportieren |
| Format option Markdown | Als Markdown exportieren |
| Per-item export tooltip | Dieses Ergebnis exportieren |
| Export success | Export wurde heruntergeladen |
| Export error | Export fehlgeschlagen. Bitte versuchen Sie es erneut. |
| PDF disclaimer footer | Generiert mit Paragraf -- keine individuelle Rechtsberatung (RDG). |

### Responsive Sidebar

| Element | Copy |
|---------|------|
| Hamburger button aria-label | Navigation oeffnen |
| Close overlay aria-label | Navigation schliessen |

### Accessibility Audit Copy

| Element | Copy |
|---------|------|
| Skip link | Zum Hauptinhalt springen |
| Search results live region | {count} Ergebnisse gefunden |
| Loading live region | Wird geladen... |
| Filter update live region | Filter angewendet, {count} Ergebnisse |
| Indexing progress live region | Indexierung: {current} von {total} |
| Snapshot operation live region | Snapshot-Vorgang laeuft... |
| Graph interaction live region | {label} ausgewaehlt |

---

## Component Inventory

### New Components

| Component | Location | Purpose |
|-----------|----------|---------|
| SnapshotSection | `components/SnapshotSection.tsx` | Snapshot list, create/restore/delete actions below IndexDashboard |
| SnapshotCard | `components/SnapshotCard.tsx` | Individual snapshot display: name, timestamp, size, action buttons |
| ExportDropdown | `components/ExportDropdown.tsx` | Format selector dropdown (PDF/Markdown) with per-page export logic |
| ExportButton | `components/ExportButton.tsx` | Per-item inline export button for ResultCard, CompareView, LookupView |
| ResponsiveSidebar | Extend `components/Sidebar.tsx` | Hamburger toggle + overlay for < 1024px screens |

### Reused Components

| Component | Usage in Phase 10 |
|-----------|-------------------|
| Dialog | Restore/delete confirmation modals |
| Button (primary) | Snapshot erstellen, Export trigger |
| Button (destructive) | Restore, Delete actions |
| Button (ghost) | Per-item export, hamburger toggle, snapshot action icons |
| Badge | Snapshot status indicator |
| Tooltip | Snapshot action icon labels (Wiederherstellen, Loeschen), export format hints |
| Card | Snapshot list items |

### Modified Components

| Component | Modification |
|-----------|-------------|
| Sidebar.tsx | Add responsive hamburger at lg breakpoint, overlay mode |
| IndexPage.tsx | Integrate SnapshotSection below existing dashboard (donut chart remains focal point, snapshots are secondary below-fold content) |
| SearchPage.tsx | Add ExportDropdown to results toolbar |
| ComparePage.tsx | Add ExportDropdown to comparison toolbar |
| LookupPage.tsx | Add ExportButton for individual lookup results |
| ResultCard.tsx | Add per-item ExportButton |
| App.tsx | Add sidebar responsive state, aria-live regions |

---

## Interaction Patterns

### Snapshot Management

1. **Create:** User clicks "Snapshot erstellen" -> button shows spinner -> success toast with snapshot name -> list refreshes with new entry at top.
2. **Restore:** User clicks restore icon on snapshot card -> Dialog opens with warning copy -> user clicks "Jetzt wiederherstellen" -> Dialog closes, page shows spinner overlay -> success toast -> page refreshes.
3. **Delete:** User clicks delete icon on snapshot card -> Dialog opens with delete warning -> user clicks "Endgueltig loeschen" -> item removed from list with fade-out -> success toast.
4. **Auto-snapshot toggle:** Switch/checkbox in SnapshotSection. State persisted in localStorage key `paragraf-auto-snapshot`. When enabled, IndexPage checks before indexing and calls create-snapshot API first.

### Export

1. **Global export:** User clicks "Alles exportieren" dropdown -> selects PDF or Markdown -> browser downloads file. No navigation change.
2. **Per-item export:** User clicks export icon on individual card -> small dropdown (PDF/Markdown) -> download triggers.
3. **PDF generation:** Client-side via JS library. Content rendered to PDF with: heading (page title + date), body content, RDG disclaimer footer on every page.
4. **Markdown generation:** Plain text blob assembled from visible data, copied to clipboard or downloaded as .md file.

### Responsive Sidebar

1. **Screen >= 1024px (lg):** Sidebar visible as fixed left panel (current behavior, unchanged).
2. **Screen < 1024px:** Sidebar hidden. Hamburger icon (Menu from lucide-react) appears top-left. Tap opens sidebar as overlay with semi-transparent backdrop. Tap backdrop or nav item closes overlay.
3. **Transition:** Sidebar slides in from left with 200ms ease-out (respects prefers-reduced-motion).

### Accessibility Focus Management

1. **Dialog open:** Focus moves to first focusable element inside Dialog (Radix handles this).
2. **Dialog close:** Focus returns to trigger element (Radix handles this).
3. **Page navigation:** Focus moves to main content area via `mainRef.current?.focus()` (existing pattern).
4. **Sidebar overlay close:** Focus returns to hamburger button.
5. **Export complete:** aria-live polite announces "Export wurde heruntergeladen".
6. **Snapshot operation complete:** aria-live polite announces result.

---

## Responsive Breakpoints

| Breakpoint | Width | Layout |
|------------|-------|--------|
| Mobile | < 768px | Single column, stacked cards, hamburger nav, full-width export button |
| Tablet | 768px - 1023px | Side-by-side where space allows, hamburger nav, snapshot cards 2-col grid |
| Desktop | >= 1024px | Fixed sidebar, content area with sidebar offset, snapshot cards 3-col or list |

### Per-Component Responsive Rules

| Component | < 768px | 768px - 1023px | >= 1024px |
|-----------|---------|----------------|-----------|
| Sidebar | Hidden, hamburger overlay | Hidden, hamburger overlay | Fixed left panel |
| SnapshotSection | Single column card stack | 2-column card grid | List/table or 3-column grid |
| ExportDropdown | Full-width button below results | Inline in toolbar | Inline in toolbar |
| FilterPanel | Full-width collapsible above results | Side panel or collapsible | Side panel (existing) |
| ResultCard | Full width, stacked metadata | Full width, inline metadata | Full width, inline metadata |
| IndexDashboard | Donut chart above stats (stacked) | Side by side | Side by side |
| GraphPage | Full viewport canvas, bottom panel | Side panel | Side panel (existing) |

---

## Accessibility Checklist (WCAG 2.1 AA)

### Structural Requirements (D-10)

- Heading hierarchy: h1 per page (page title), h2 for sections (Snapshots, Ergebnisse, Filter), h3 for subsections
- Landmark regions: `<main>` for content, `<nav>` for sidebar, `<aside>` for filter panel
- All images/icons: `aria-hidden="true"` on decorative, meaningful alt/aria-label on functional
- Form controls: associated `<label>` or `aria-label` on every input, select, checkbox

### Dynamic Content (D-11)

All aria-live regions use `aria-live="polite"` unless specified:

| Area | Level | Content |
|------|-------|---------|
| Search results count | polite | "{count} Ergebnisse gefunden" |
| Loading indicators | polite | "Wird geladen..." |
| Filter updates | polite | "Filter angewendet, {count} Ergebnisse" |
| Indexing progress | polite | "Indexierung: {current} von {total}" |
| Snapshot operations | polite | Operation status messages |
| Error messages | assertive | Error description |
| Graph node selection | polite | "{label} ausgewaehlt" |
| Tooltip content | polite | Tooltip text (Radix handles) |

### Keyboard Navigation

- All interactive elements reachable via Tab
- Escape closes overlays (Dialog, sidebar overlay, dropdowns)
- Enter/Space activates buttons
- Arrow keys navigate within dropdown menus
- Existing Ctrl+1-7 shortcuts preserved
- Focus-visible: 3px solid primary-500, offset 2px (existing)

### Contrast Ratios (minimum 4.5:1 for text, 3:1 for large text)

| Element | Light Mode | Dark Mode |
|---------|-----------|-----------|
| Body text on background | neutral-700 on neutral-50 (14.5:1) | neutral-200 on neutral-950 (15.3:1) |
| Muted text (timestamps, sizes) | neutral-400 on white (3.9:1 — use neutral-500 for AA) | neutral-400 on neutral-900 (5.5:1) |
| Primary button text | white on primary-600 (8.6:1) | white on primary-600 (8.6:1) |
| Destructive button text | white on error-500 (4.6:1) | white on error-500 (4.6:1) |
| Link text | primary-600 on neutral-50 (7.8:1) | primary-400 on neutral-950 (5.1:1) |

Note: Muted text in light mode uses `text-neutral-500` (not `text-neutral-400`) to meet WCAG AA 4.5:1 minimum. In dark mode, `text-neutral-400` on `neutral-900` passes at 5.5:1.

### Touch Targets (D-14)

- All buttons: minimum 44px height (Button size md = h-10/40px -> needs min-h-[44px] override or use size lg = h-12/48px)
- Sidebar nav items: minimum 44px height
- Snapshot action icons: icon-only ghost buttons wrapped in 44px min tap area, with Tooltip for label
- Export dropdown items: minimum 44px height per option
- Checkbox/toggle controls: 44px tap area (padding around input)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| No shadcn | not applicable | not applicable |
| npm: PDF library (Claude's discretion) | jspdf or html2pdf.js | Standard npm package -- no registry gate needed |

Note: No shadcn registry is used. The project uses a manual component system with cva + cn() + Radix primitives. PDF library is a standard npm dependency chosen at implementation time per D-07.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
