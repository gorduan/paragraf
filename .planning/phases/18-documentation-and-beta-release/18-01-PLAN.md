---
phase: 18-documentation-and-beta-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/src/paragraf/__init__.py
  - frontend/package.json
  - desktop/package.json
  - docker-compose.yml
  - .env.example
  - CLAUDE.md
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "Version 0.9-beta (bzw. PEP 440 / npm semver Aequivalent) erscheint in allen relevanten Dateien"
    - "Kein 'v2' oder 'Paragraf v2' mehr in Projekt-Beschreibungen (nur noch 'Paragraf')"
    - "CLAUDE.md Projektbeschreibung reflektiert aktuellen Stand ohne v2-Referenzen"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "Python package version"
      contains: 'version = "0.9b0"'
    - path: "backend/src/paragraf/__init__.py"
      provides: "Runtime version"
      contains: '__version__ = "0.9b0"'
    - path: "frontend/package.json"
      provides: "Frontend version"
      contains: '"version": "0.9.0-beta"'
    - path: "desktop/package.json"
      provides: "Desktop app version"
      contains: '"version": "0.9.0-beta"'
    - path: "CLAUDE.md"
      provides: "Project conventions without v2 references"
    - path: ".env.example"
      provides: "Env template without v2 references"
  key_links: []
---

<objective>
Update all version strings to 0.9-beta and remove "v2" branding from project metadata.

Purpose: Per D-04, version must change from current values (0.2.0 / 2.0.0) to 0.9-beta everywhere. Per D-07, "v2" must be removed from project title and descriptions. This is a prerequisite for all documentation work.
Output: All version files updated, CLAUDE.md cleaned up, .env.example header updated.
</objective>

<execution_context>
@E:/Claude Projekte/Tobias/paragraf v2/.claude/get-shit-done/workflows/execute-plan.md
@E:/Claude Projekte/Tobias/paragraf v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-documentation-and-beta-release/18-CONTEXT.md
@.planning/phases/18-documentation-and-beta-release/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version string sweep across all project files</name>
  <files>backend/pyproject.toml, backend/src/paragraf/__init__.py, frontend/package.json, desktop/package.json, docker-compose.yml</files>
  <read_first>
    - backend/pyproject.toml (current version: line 3, "0.2.0")
    - backend/src/paragraf/__init__.py (current version: line 3, "0.2.0")
    - frontend/package.json (current version: line 3, "0.2.0")
    - desktop/package.json (current version: line 3, "2.0.0")
    - docker-compose.yml (check for any version labels)
  </read_first>
  <action>
    Update version strings in all 5 files. Use the correct format per ecosystem (per RESEARCH.md Pitfall 1 and 2):

    1. `backend/pyproject.toml` line 3: change `version = "0.2.0"` to `version = "0.9b0"` (PEP 440 format — "0.9-beta" is NOT valid PEP 440)
    2. `backend/src/paragraf/__init__.py` line 3: change `__version__ = "0.2.0"` to `__version__ = "0.9b0"` (must match pyproject.toml)
    3. `frontend/package.json` line 3: change `"version": "0.2.0"` to `"version": "0.9.0-beta"` (npm requires 3-part semver)
    4. `desktop/package.json` line 3: change `"version": "2.0.0"` to `"version": "0.9.0-beta"` (npm requires 3-part semver)
    5. `docker-compose.yml`: search for any version labels or "v2" references and update to "0.9-beta" or remove

    After edits, run a verification grep to ensure no "0.2.0" or "2.0.0" version strings remain in these files.
    Also run: `grep -rn "0\.2\.0\|2\.0\.0" --include="*.py" --include="*.json" --include="*.toml" --include="*.yml" backend/pyproject.toml backend/src/paragraf/__init__.py frontend/package.json desktop/package.json docker-compose.yml` — must return empty.
  </action>
  <verify>
    <automated>cd "E:/Claude Projekte/Tobias/paragraf v2" && grep "0.9b0" backend/pyproject.toml backend/src/paragraf/__init__.py && grep "0.9.0-beta" frontend/package.json desktop/package.json && ! grep -q "0\.2\.0" backend/pyproject.toml backend/src/paragraf/__init__.py frontend/package.json && ! grep -q "2\.0\.0" desktop/package.json</automated>
  </verify>
  <acceptance_criteria>
    - backend/pyproject.toml contains `version = "0.9b0"`
    - backend/src/paragraf/__init__.py contains `__version__ = "0.9b0"`
    - frontend/package.json contains `"version": "0.9.0-beta"`
    - desktop/package.json contains `"version": "0.9.0-beta"`
    - No file in the list contains "0.2.0" or "2.0.0" as a version string
  </acceptance_criteria>
  <done>All 5 files have correct 0.9-beta version strings in ecosystem-appropriate format. Old versions completely gone.</done>
</task>

<task type="auto">
  <name>Task 2: Remove "v2" branding from CLAUDE.md and .env.example</name>
  <files>CLAUDE.md, .env.example</files>
  <read_first>
    - CLAUDE.md (full file — multiple "v2" references to find and update)
    - .env.example (header line contains "Paragraf v2")
  </read_first>
  <action>
    Update CLAUDE.md — remove "v2" from project-facing descriptions while keeping technical references intact:

    1. Line 1: Change `# Paragraf v2 – Docker Web-App fuer deutsches Recht` to `# Paragraf – RAG-basierte Rechtsrecherche fuer deutsches Recht` (per D-07)
    2. Line ~20 directory tree: Change `paragraf v2/` to `paragraf/` in the ASCII tree
    3. Line ~130 Project section: Change `**Paragraf v2 — Volles Qdrant-Potenzial**` to `**Paragraf 0.9-beta — RAG-basierte Rechtsrecherche fuer deutsches Recht**`
    4. Update the project description paragraph after the title to reflect honest positioning per D-01/D-02/D-03: mention that MCP-Server is the primary strength, frontend is a demo interface, and the system is thorough (not fast).
    5. Constraints section: Change `**Branch**: Alle Commits auf Branch "Docker-only"` — keep as is, this is still correct.
    6. DO NOT change technical references that happen to contain "v2" as part of a library name (e.g., "bge-reranker-v2-m3", "Pydantic v2"). These are library version identifiers, not project branding.

    Update .env.example:
    1. Line 1: Change `# ── Paragraf v2 – Docker Environment` to `# ── Paragraf – Docker Environment`

    Verification: run `grep -n "Paragraf v2" CLAUDE.md .env.example` — should return empty (library refs like "bge-reranker-v2-m3" do NOT match "Paragraf v2").
  </action>
  <verify>
    <automated>cd "E:/Claude Projekte/Tobias/paragraf v2" && ! grep -q "Paragraf v2" CLAUDE.md .env.example && grep -q "# Paragraf" CLAUDE.md && grep -q "0.9-beta" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md line 1 starts with `# Paragraf` (not `# Paragraf v2`)
    - CLAUDE.md contains "0.9-beta" in the project section
    - CLAUDE.md does NOT contain the string "Paragraf v2" anywhere
    - .env.example line 1 does NOT contain "Paragraf v2"
    - Technical library references like "bge-reranker-v2-m3" and "Pydantic v2" are preserved unchanged
    - CLAUDE.md still contains "bge-reranker-v2-m3" (library name preserved)
  </acceptance_criteria>
  <done>"Paragraf v2" branding removed from all project metadata. Library version references preserved. CLAUDE.md reflects 0.9-beta positioning.</done>
</task>

</tasks>

<verification>
Run after both tasks:
```bash
# V-01: Version strings consistent
grep -n "0.9" backend/pyproject.toml backend/src/paragraf/__init__.py frontend/package.json desktop/package.json

# V-05: No "v2" remnants in project descriptions
grep -n "Paragraf v2" CLAUDE.md .env.example README.md 2>/dev/null
# Should return empty or only README.md (rewritten in Plan 02)
```
</verification>

<success_criteria>
- All version strings updated to 0.9-beta (ecosystem-appropriate format)
- "Paragraf v2" branding removed from CLAUDE.md and .env.example
- Library references containing "v2" (bge-reranker-v2-m3, Pydantic v2) are preserved
- No regression in existing functionality (version change is metadata only)
</success_criteria>

<output>
After completion, create `.planning/phases/18-documentation-and-beta-release/18-01-SUMMARY.md`
</output>
