---
phase: 03-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/src/components/ui/Dialog.tsx
  - frontend/src/components/ui/Select.tsx
  - frontend/src/components/ui/Tooltip.tsx
  - frontend/src/components/ui/Tabs.tsx
  - frontend/src/pages/SearchPage.tsx
  - frontend/src/components/Sidebar.tsx
  - frontend/src/components/ResultCard.tsx
  - frontend/src/components/SearchBar.tsx
autonomous: false
requirements:
  - UI-01

must_haves:
  truths:
    - "Dialog, Select, Tooltip, Tabs primitives exist using Radix UI headless components"
    - "SearchPage uses Button, Card, Input, Badge primitives instead of inline Tailwind"
    - "Sidebar uses token-based spacing and color classes"
    - "Dark mode toggle continues working after migration"
    - "Keyboard shortcuts (Ctrl+1-7) continue working after migration"
    - "All accessibility attributes preserved (aria-*, role, sr-only)"
    - "Frontend builds without TypeScript errors"
  artifacts:
    - path: "frontend/src/components/ui/Dialog.tsx"
      provides: "Radix-based dialog with focus trap and overlay"
      exports: ["Dialog", "DialogTrigger", "DialogContent", "DialogTitle", "DialogDescription"]
    - path: "frontend/src/components/ui/Select.tsx"
      provides: "Radix-based select dropdown"
      exports: ["Select", "SelectTrigger", "SelectContent", "SelectItem", "SelectGroup", "SelectLabel"]
    - path: "frontend/src/components/ui/Tooltip.tsx"
      provides: "Radix-based tooltip"
      exports: ["Tooltip", "TooltipTrigger", "TooltipContent", "TooltipProvider"]
    - path: "frontend/src/components/ui/Tabs.tsx"
      provides: "Radix-based tabs"
      exports: ["Tabs", "TabsList", "TabsTrigger", "TabsContent"]
    - path: "frontend/src/pages/SearchPage.tsx"
      provides: "Migrated SearchPage using design system primitives"
    - path: "frontend/src/components/Sidebar.tsx"
      provides: "Migrated Sidebar using token-based classes"
  key_links:
    - from: "frontend/src/components/ui/Dialog.tsx"
      to: "@radix-ui/react-dialog"
      via: "import * as DialogPrimitive"
      pattern: "import.*@radix-ui/react-dialog"
    - from: "frontend/src/pages/SearchPage.tsx"
      to: "frontend/src/components/ui/Badge.tsx"
      via: "import Badge"
      pattern: "import.*Badge.*from.*@/components/ui/Badge"
    - from: "frontend/src/components/ResultCard.tsx"
      to: "frontend/src/components/ui/Badge.tsx"
      via: "import Badge"
      pattern: "import.*Badge.*from.*@/components/ui/Badge"
---

<objective>
Build 4 Radix-based primitives (Dialog, Select, Tooltip, Tabs) and migrate SearchPage + Sidebar to use the new design system primitives and tokens.

Purpose: Complete the primitive component set (D-04, D-06) and validate the design system works in production pages (D-07). Migration proves tokens and primitives integrate correctly.
Output: 4 Radix primitives, migrated SearchPage/Sidebar/ResultCard/SearchBar using new primitives.
</objective>

<execution_context>
@E:/Claude Projekte/Tobias/paragraf v2/.claude/get-shit-done/workflows/execute-plan.md
@E:/Claude Projekte/Tobias/paragraf v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-system-foundation/03-CONTEXT.md
@.planning/phases/03-design-system-foundation/03-RESEARCH.md
@.planning/phases/03-design-system-foundation/03-UI-SPEC.md
@.planning/phases/03-design-system-foundation/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (read 03-01-SUMMARY.md for actual state) -->

From frontend/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From frontend/src/components/ui/Button.tsx:
```typescript
export function Button({ className, variant, size, ...props }: ButtonProps): JSX.Element;
// variant: "primary" | "secondary" | "ghost" | "destructive"
// size: "sm" | "md" | "lg"
```

From frontend/src/components/ui/Card.tsx:
```typescript
export function Card({ className, variant, ...props }: CardProps): JSX.Element;
// variant: "default" | "interactive"
```

From frontend/src/components/ui/Input.tsx:
```typescript
export const Input: React.ForwardRefExoticComponent<InputProps>;
// variant: "default" | "error"
```

From frontend/src/components/ui/Badge.tsx:
```typescript
export function Badge({ className, variant, ...props }: BadgeProps): JSX.Element;
// variant: "default" | "primary" | "success" | "warning" | "error"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build 4 Radix-based primitives (Dialog, Select, Tooltip, Tabs)</name>
  <files>
    frontend/src/components/ui/Dialog.tsx,
    frontend/src/components/ui/Select.tsx,
    frontend/src/components/ui/Tooltip.tsx,
    frontend/src/components/ui/Tabs.tsx
  </files>
  <read_first>
    frontend/src/lib/utils.ts,
    frontend/src/styles/index.css,
    frontend/package.json,
    .planning/phases/03-design-system-foundation/03-UI-SPEC.md,
    .planning/phases/03-design-system-foundation/03-RESEARCH.md
  </read_first>
  <action>
**Dialog.tsx** (per D-06, UI-SPEC Dialog section):
- `import * as DialogPrimitive from "@radix-ui/react-dialog"`
- `import { X } from "lucide-react"`
- `import { cn } from "@/lib/utils"`
- Export named components: `Dialog` (wraps Root), `DialogTrigger`, `DialogContent`, `DialogTitle`, `DialogDescription`, `DialogClose`
- `DialogContent` wraps `DialogPrimitive.Portal` > `DialogPrimitive.Overlay` + `DialogPrimitive.Content`
- Overlay: `"fixed inset-0 z-50 bg-black/50 data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=open]:fade-in-0 data-[state=closed]:fade-out-0"`
- Content: `"fixed z-50 left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 bg-white dark:bg-slate-800 shadow-lg rounded-lg p-lg max-w-[480px] w-full data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=open]:fade-in-0 data-[state=open]:zoom-in-95 data-[state=closed]:fade-out-0 data-[state=closed]:zoom-out-95"`
- Close button: absolute top-4 right-4, renders X icon (size=16) with `<span className="sr-only">Schliessen</span>`
- DialogTitle: `"text-heading font-semibold text-slate-900 dark:text-slate-100"`
- DialogDescription: `"text-body text-slate-500 dark:text-slate-400 mt-sm"`
- All sub-components accept className prop merged via cn()

**Select.tsx** (per D-06, UI-SPEC Select section):
- `import * as SelectPrimitive from "@radix-ui/react-select"`
- `import { Check, ChevronDown, ChevronUp } from "lucide-react"`
- Export: `Select` (Root), `SelectTrigger`, `SelectValue`, `SelectContent`, `SelectItem`, `SelectGroup`, `SelectLabel`, `SelectSeparator`
- Trigger styled like Input: `"flex h-10 w-full items-center justify-between px-xs2 bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-600 rounded-md text-body placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-primary-500 disabled:opacity-50 disabled:cursor-not-allowed"`
- Content: `"bg-white dark:bg-slate-800 shadow-lg rounded-md border border-slate-200 dark:border-slate-700 overflow-hidden z-50"`
- Item: `"relative flex cursor-pointer select-none items-center h-9 px-xs2 text-body outline-none hover:bg-slate-100 dark:hover:bg-slate-700 data-[state=checked]:bg-primary-50 dark:data-[state=checked]:bg-primary-900/30 data-[disabled]:opacity-50 data-[disabled]:pointer-events-none"`
- ItemIndicator: Check icon (size=14) on selected item
- ScrollUpButton/ScrollDownButton with ChevronUp/ChevronDown

**Tooltip.tsx** (per D-06, UI-SPEC Tooltip section):
- `import * as TooltipPrimitive from "@radix-ui/react-tooltip"`
- Export: `TooltipProvider` (Root Provider, wraps app), `Tooltip` (Root), `TooltipTrigger`, `TooltipContent`
- Content: `"z-50 bg-slate-900 dark:bg-slate-100 text-white dark:text-slate-900 text-caption px-sm py-1.5 rounded-sm shadow-sm max-w-[200px] data-[state=delayed-open]:animate-in data-[state=closed]:animate-out data-[state=delayed-open]:fade-in-0 data-[state=closed]:fade-out-0"`
- Default delayDuration: 400
- Include `<TooltipPrimitive.Arrow>` with matching background color

**Tabs.tsx** (per D-06, UI-SPEC Tabs section):
- `import * as TabsPrimitive from "@radix-ui/react-tabs"`
- Export: `Tabs` (Root), `TabsList`, `TabsTrigger`, `TabsContent`
- TabsList: `"flex border-b border-slate-200 dark:border-slate-700"`
- TabsTrigger: `"h-10 px-md text-body text-slate-500 hover:text-slate-700 dark:hover:text-slate-300 data-[state=active]:text-primary-600 dark:data-[state=active]:text-primary-400 data-[state=active]:border-b-2 data-[state=active]:border-primary-600 dark:data-[state=active]:border-primary-400 transition-colors"`
- TabsContent: `"pt-md"`
- All sub-components accept className merged via cn()
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ui/Dialog.tsx contains `import * as DialogPrimitive from "@radix-ui/react-dialog"`
    - frontend/src/components/ui/Dialog.tsx contains `export function DialogContent`
    - frontend/src/components/ui/Dialog.tsx contains `Schliessen`
    - frontend/src/components/ui/Dialog.tsx contains `sr-only`
    - frontend/src/components/ui/Select.tsx contains `import * as SelectPrimitive from "@radix-ui/react-select"`
    - frontend/src/components/ui/Select.tsx contains `export function SelectContent`
    - frontend/src/components/ui/Select.tsx contains `data-[state=checked]`
    - frontend/src/components/ui/Tooltip.tsx contains `import * as TooltipPrimitive from "@radix-ui/react-tooltip"`
    - frontend/src/components/ui/Tooltip.tsx contains `export function TooltipContent`
    - frontend/src/components/ui/Tooltip.tsx contains `delayDuration`
    - frontend/src/components/ui/Tabs.tsx contains `import * as TabsPrimitive from "@radix-ui/react-tabs"`
    - frontend/src/components/ui/Tabs.tsx contains `export function TabsTrigger`
    - frontend/src/components/ui/Tabs.tsx contains `data-[state=active]`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>4 Radix-based primitives (Dialog, Select, Tooltip, Tabs) created with Tailwind styling, cn() integration, and accessible defaults. All export named components matching Radix sub-component pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate SearchPage, SearchBar, ResultCard, and Sidebar to design system</name>
  <files>
    frontend/src/pages/SearchPage.tsx,
    frontend/src/components/Sidebar.tsx,
    frontend/src/components/ResultCard.tsx,
    frontend/src/components/SearchBar.tsx
  </files>
  <read_first>
    frontend/src/pages/SearchPage.tsx,
    frontend/src/components/Sidebar.tsx,
    frontend/src/components/ResultCard.tsx,
    frontend/src/components/SearchBar.tsx,
    frontend/src/components/ui/Button.tsx,
    frontend/src/components/ui/Card.tsx,
    frontend/src/components/ui/Input.tsx,
    frontend/src/components/ui/Badge.tsx,
    .planning/phases/03-design-system-foundation/03-UI-SPEC.md
  </read_first>
  <action>
Per D-07: Migrate SearchPage + Sidebar as reference migration. Also migrate ResultCard and SearchBar since they are child components of SearchPage that contain inline styling replaceable by primitives. Per Pitfall 6 in RESEARCH: do NOT replace the native `<select>` in SearchBar with Radix Select -- keep it as-is.

**SearchBar.tsx migration:**
- Add import: `import { Button } from "@/components/ui/Button"`
- Replace the search submit button (line 164-169 in current file: `<button onClick={() => doSearch(query)} className="px-5 py-3 bg-primary-600...">Suchen</button>`) with `<Button onClick={() => doSearch(query)} size="lg">Suche starten</Button>` (per UI-SPEC copywriting contract: CTA is "Suche starten", not "Suchen")
- The search input field keeps its current inline styling because it has specialized layout needs (icon overlay, clear button). Do NOT replace with Input primitive -- the Input primitive is for simpler contexts.
- The native `<select>` for law filtering stays unchanged (per Pitfall 6).
- All existing ARIA attributes, roles, and accessibility patterns MUST be preserved exactly.

**ResultCard.tsx migration:**
- Add imports: `import { Badge } from "@/components/ui/Badge"` and `import { Button } from "@/components/ui/Button"`
- Replace the Gesetz badge (line 60-62 in current: `<span className="px-2 py-0.5 bg-primary-100 dark:bg-primary-900/30 text-primary-700 dark:text-primary-300 text-xs rounded-full" aria-hidden="true">{result.gesetz}</span>`) with `<Badge variant="primary" aria-hidden="true">{result.gesetz}</Badge>`
- Replace the action bar buttons (Kopieren, Merken, Vergleichen at lines 100-133) with `<Button variant="ghost" size="sm">` pattern. Each action button gets: `<Button variant="ghost" size="sm" onClick={handler} aria-label={ariaLabel} className="gap-1">`. Preserve all existing aria-label, aria-pressed, aria-live attributes exactly.
- The article wrapper keeps its current inline classes -- do NOT replace with Card primitive because ResultCard has a complex expand/collapse structure that doesn't match the simple Card layout.
- All existing ARIA attributes (aria-expanded, aria-label, role="toolbar") MUST be preserved exactly.

**SearchPage.tsx migration:**
- Replace heading sizing: change `text-2xl font-bold` to `text-heading font-semibold` (per typography tokens)
- Replace subtitle: change `text-sm text-slate-500` to `text-body text-neutral-500` (using neutral token names)
- Replace error state container: change `bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 rounded-lg` to `bg-error-50 dark:bg-error-900/20 border border-error-200 dark:border-error-800 rounded-md` (using semantic error tokens and radius token)
- Replace padding values: `p-6` stays (standard Tailwind numeric, still works), `py-12` stays, `mt-4`/`mt-6`/`mt-8`/`mt-12` stay (standard numeric spacing still works alongside named tokens)
- Replace `text-sm` with `text-body` where it appears in result count and empty state text
- All existing ARIA attributes (role="status", aria-live, role="alert", role="list", aria-label) MUST be preserved exactly.
- The Loader spinner keeps its existing `text-primary-500` class (token-based after plan 01 swapped to indigo).

**Sidebar.tsx migration:**
- Replace hardcoded color classes with token-based equivalents where beneficial:
  - Logo `text-xl font-bold` to `text-heading font-semibold`
  - Subtitle `text-xs` to `text-caption`
  - Nav item `text-sm` to `text-body`
  - Active state `bg-primary-100 dark:bg-primary-900/30 text-primary-700 dark:text-primary-300` stays (these ARE token-based after plan 01)
  - Inactive state `text-slate-600 dark:text-slate-400` stays (standard Tailwind slate still works)
- All existing ARIA attributes (aria-label, aria-current, aria-hidden) MUST be preserved exactly.
- ServerStatus component import and usage stays unchanged.
- The keyboard shortcut kbd elements stay with their current styling.

**CRITICAL CONSTRAINTS:**
- Do NOT break any functionality. This is a styling migration only.
- Do NOT change component props interfaces or state management.
- Do NOT remove or alter any ARIA attribute, role, or accessibility pattern.
- Do NOT replace the native `<select>` in SearchBar.
- The import paths for SearchBar, ResultCard, Disclaimer, and api remain unchanged.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/SearchBar.tsx contains `import { Button } from "@/components/ui/Button"`
    - frontend/src/components/SearchBar.tsx contains `Suche starten` (per UI-SPEC copywriting)
    - frontend/src/components/SearchBar.tsx contains `<select` (native select preserved per Pitfall 6)
    - frontend/src/components/ResultCard.tsx contains `import { Badge } from "@/components/ui/Badge"`
    - frontend/src/components/ResultCard.tsx contains `<Badge variant="primary"`
    - frontend/src/components/ResultCard.tsx contains `import { Button } from "@/components/ui/Button"`
    - frontend/src/components/ResultCard.tsx contains `aria-expanded` (accessibility preserved)
    - frontend/src/components/ResultCard.tsx contains `role="toolbar"` (accessibility preserved)
    - frontend/src/pages/SearchPage.tsx contains `text-heading` (typography token)
    - frontend/src/pages/SearchPage.tsx contains `bg-error-50` (semantic color token)
    - frontend/src/pages/SearchPage.tsx contains `role="status"` (accessibility preserved)
    - frontend/src/pages/SearchPage.tsx contains `role="alert"` (accessibility preserved)
    - frontend/src/components/Sidebar.tsx contains `text-caption` (typography token)
    - frontend/src/components/Sidebar.tsx contains `aria-current` (accessibility preserved)
    - `npm run build` exits 0 (tsc + vite build both pass)
  </acceptance_criteria>
  <done>SearchPage, SearchBar, ResultCard, and Sidebar migrated to use design system primitives and tokens. All accessibility attributes preserved. Native select in SearchBar preserved. Build passes without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of design system migration</name>
  <files>none</files>
  <action>
Human visually verifies the design system migration in the browser. Start the frontend dev server and check that:
1. SearchPage "Suche starten" button is indigo (not blue)
2. Search input has proper focus ring
3. Result card Gesetz badges show in indigo tint
4. Action buttons (Kopieren, Merken) are ghost-styled
5. Sidebar navigation items are properly spaced with indigo accent on active item
6. Dark mode toggle switches all colors correctly
7. Keyboard shortcuts Ctrl+1 through Ctrl+7 navigate pages
8. Browser console has no React errors or warnings
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Human has visually confirmed the design system migration looks correct in both light and dark mode, with no functional regressions.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` exits 0 (full TypeScript + Vite build)
- `cd frontend && npx vitest run --reporter=verbose` exits 0 (all tests pass)
- All 8 primitive components exist in `frontend/src/components/ui/`
- SearchPage and Sidebar use design system tokens/primitives
- All ARIA/accessibility attributes preserved
- Dark mode continues working
- Keyboard shortcuts continue working
</verification>

<success_criteria>
- 8 primitive components created: Button, Card, Input, Badge (Plan 01), Dialog, Select, Tooltip, Tabs (this plan)
- SearchPage uses Button, Badge, and typography tokens
- ResultCard uses Badge and Button primitives
- SearchBar uses Button primitive with "Suche starten" CTA text
- Sidebar uses typography tokens
- All existing accessibility patterns preserved (ARIA, roles, focus-visible, reduced-motion, high-contrast, skip-link)
- Frontend builds and all tests pass
- Human verifies visual correctness in browser
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-system-foundation/03-02-SUMMARY.md`
</output>
